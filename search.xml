<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>基于PCA的人脸识别</title>
    <url>/2020/05/05/PCA_Face_Recognize/</url>
    <content><![CDATA[<p><strong>PCA旨在利用降维的思想，把多指标转化为少数几个综合指标。</strong></p>
<h1 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h1><p>​        PCA(Principal Component Analysis)，即主成分分析方法，是一种使用最广泛的数据降维算法。PCA的主要思想是将n维特征映射到k维上，这k维是全新的正交特征也被称为主成分，是在原有n维特征的基础上重新构造出来的k维特征。PCA的工作就是从原始的空间中顺序地找一组相互正交的坐标轴，新的坐标轴的选择与数据本身是密切相关的。其中，第一个新坐标轴选择是原始数据中方差最大的方向，第二个新坐标轴选取是与第一个坐标轴正交的平面中使得方差最大的，第三个轴是与第1,2个轴正交的平面中方差最大的。依次类推，可以得到n个这样的坐标轴。通过这种方式获得的新的坐标轴，我们发现，大部分方差都包含在前面k个坐标轴中，后面的坐标轴所含的方差几乎为0。于是，我们可以忽略余下的坐标轴，只保留前面k个含有绝大部分方差的坐标轴。事实上，这相当于只保留包含绝大部分方差的维度特征，而忽略包含方差几乎为0的特征维度，实现对数据特征的降维处理。</p>
<h1 id="数学知识"><a href="#数学知识" class="headerlink" title="数学知识"></a>数学知识</h1><h2 id="协方差与协方差矩阵"><a href="#协方差与协方差矩阵" class="headerlink" title="协方差与协方差矩阵"></a>协方差与协方差矩阵</h2><h3 id="样本均值"><a href="#样本均值" class="headerlink" title="样本均值"></a>样本均值</h3><p><img alt data-src="/img/PCA_Face_Recognize/mean_value.jpg" class="lazyload"></p>
<h3 id="样本方差"><a href="#样本方差" class="headerlink" title="样本方差"></a>样本方差</h3><p><img alt data-src="/img/PCA_Face_Recognize/variance.jpg" class="lazyload"></p>
<h3 id="协方差"><a href="#协方差" class="headerlink" title="协方差"></a>协方差</h3><p><img alt data-src="/img/PCA_Face_Recognize/covariance.jpg" class="lazyload"></p>
<h3 id="协方差矩阵"><a href="#协方差矩阵" class="headerlink" title="协方差矩阵"></a>协方差矩阵</h3><p><img alt data-src="/img/PCA_Face_Recognize/covariance_matrix.jpg" class="lazyload"></p>
<h2 id="特征值与特征向量"><a href="#特征值与特征向量" class="headerlink" title="特征值与特征向量"></a>特征值与特征向量</h2><p>​    对于给定矩阵A，寻找一个常数λ（可以为复数）和非零向量x，使得向量x被矩阵A作用后所得的向量Ax与原向量x平行，并且满足Ax=λx。其中<strong>常数λ</strong>称为<strong>特征值</strong>，而<strong>非零向量x</strong>称为<strong>特征向量</strong>。</p>
<h1 id="PCA算法步骤"><a href="#PCA算法步骤" class="headerlink" title="PCA算法步骤"></a>PCA算法步骤</h1><p><img alt data-src="/img/PCA_Face_Recognize/code.jpg" class="lazyload"></p>
<h1 id="人脸识别代码"><a href="#人脸识别代码" class="headerlink" title="人脸识别代码"></a>人脸识别代码</h1><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#coding:GBK</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> os</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> numpy <span class="keyword">import</span> *</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> cv2</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> pylab <span class="keyword">import</span> mpl</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">mpl.rcParams[<span class="string">'font.sans-serif'</span>] = [<span class="string">'SimHei'</span>]</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">#训练的图片数，每个人总共有十张图片，其他图片用来进行测试</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">pic_train_num = <span class="number">9</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment"># 图片矢量化</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">img2vector</span><span class="params">(image)</span>:</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    img = cv2.imread(image, <span class="number">0</span>)  <span class="comment"># 读取图片</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    rows, cols = img.shape  <span class="comment">#获取图片的像素</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    imgVector = np.zeros((<span class="number">1</span>, rows * cols))</span></pre></td></tr><tr><td class="code"><pre><span class="line">    imgVector = np.reshape(img, (<span class="number">1</span>, rows * cols))<span class="comment">#使用imgVector变量作为一个向量存储图片矢量化信息，初始值均设置为0</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> imgVector</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">orlpath = <span class="string">"ORL"</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment"># 读入人脸库,每个人随机选择k张作为训练集,其余构成测试集</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">load_orl</span><span class="params">(k)</span>:</span><span class="comment">#参数K代表选择K张图片作为训练图片使用</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="string">'''</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="string">    对训练数据集进行数组初始化，用0填充，每张图片尺寸都定为112*92,</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="string">    现在共有40个人，每个人都选择k张，则整个训练集大小为40*k,112*92</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="string">    '''</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    train_face = np.zeros((<span class="number">40</span> * k, <span class="number">112</span> * <span class="number">92</span>))</span></pre></td></tr><tr><td class="code"><pre><span class="line">    train_label = np.zeros(<span class="number">40</span> * k)  <span class="comment"># [0,0,.....0](共40*k个0)</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    test_face = np.zeros((<span class="number">40</span> * (<span class="number">10</span> - k), <span class="number">112</span> * <span class="number">92</span>))</span></pre></td></tr><tr><td class="code"><pre><span class="line">    test_label = np.zeros(<span class="number">40</span> * (<span class="number">10</span> - k))</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="comment"># sample=random.sample(range(10),k)#每个人都有的10张照片中，随机选取k张作为训练样本(10个里面随机选取K个成为一个列表)</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    sample = random.permutation(<span class="number">10</span>) + <span class="number">1</span>  <span class="comment"># 随机排序1-10 (0-9）+1</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">40</span>):  <span class="comment"># 共有40个人</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        people_num = i + <span class="number">1</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> range(<span class="number">10</span>):  <span class="comment"># 每个人都有10张照片</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">            image = orlpath + <span class="string">'/s'</span> + str(people_num) + <span class="string">'/'</span> + str(sample[j]) + <span class="string">'.jpg'</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="comment"># 读取图片并进行矢量化</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">            img = img2vector(image)</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">if</span> j &lt; k:</span></pre></td></tr><tr><td class="code"><pre><span class="line">                <span class="comment"># 构成训练集</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">                train_face[i * k + j, :] = img</span></pre></td></tr><tr><td class="code"><pre><span class="line">                train_label[i * k + j] = people_num</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">else</span>:</span></pre></td></tr><tr><td class="code"><pre><span class="line">                <span class="comment"># 构成测试集</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">                test_face[i * (<span class="number">10</span> - k) + (j - k), :] = img</span></pre></td></tr><tr><td class="code"><pre><span class="line">                test_label[i * (<span class="number">10</span> - k) + (j - k)] = people_num</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> train_face, train_label, test_face, test_label</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment"># 定义PCA算法</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">PCA</span><span class="params">(data, r)</span>:</span><span class="comment">#降低到r维</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    data = np.float32(np.mat(data))</span></pre></td></tr><tr><td class="code"><pre><span class="line">    rows, cols = np.shape(data)</span></pre></td></tr><tr><td class="code"><pre><span class="line">    data_mean = np.mean(data, <span class="number">0</span>)  <span class="comment"># 对列求平均值</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    X = data - np.tile(data_mean, (rows, <span class="number">1</span>))  <span class="comment"># 将所有样例减去对应均值得到A</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    C = X * X.T  <span class="comment"># 得到协方差矩阵</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    D, V = np.linalg.eig(C)  <span class="comment"># 求协方差矩阵的特征值和特征向量</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    P = V[:, <span class="number">0</span>:r]  <span class="comment"># 按列取前r个特征向量</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    P = X.T * P  <span class="comment"># 小矩阵特征向量向大矩阵特征向量过渡</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(r):</span></pre></td></tr><tr><td class="code"><pre><span class="line">        P[:, i] = P[:, i] / np.linalg.norm(P[:, i])  <span class="comment"># 特征向量归一化</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    Y = X * P</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> Y, data_mean, P</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">face_recognize</span><span class="params">()</span>:</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">for</span> r <span class="keyword">in</span> range(<span class="number">10</span>, <span class="number">41</span>, <span class="number">10</span>):  <span class="comment"># 最多降到40维,即选取前40个主成分（因为当k=1时，只有40维)</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        print(<span class="string">"当降维到%d时"</span> % (r))</span></pre></td></tr><tr><td class="code"><pre><span class="line">        x_value = []</span></pre></td></tr><tr><td class="code"><pre><span class="line">        y_value = []</span></pre></td></tr><tr><td class="code"><pre><span class="line">        train_face, train_label, test_face, test_label = load_orl(pic_train_num)  <span class="comment"># 得到数据集</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="comment"># 利用PCA算法进行训练</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        data_train_new, data_mean, P = PCA(train_face, r)</span></pre></td></tr><tr><td class="code"><pre><span class="line">        num_train = data_train_new.shape[<span class="number">0</span>]  <span class="comment"># 训练脸总数</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        num_test = test_face.shape[<span class="number">0</span>]  <span class="comment"># 测试脸总数</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        temp_face = test_face - np.tile(data_mean, (num_test, <span class="number">1</span>))</span></pre></td></tr><tr><td class="code"><pre><span class="line">        data_test_new = temp_face * P  <span class="comment"># 得到测试脸在特征向量下的数据</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        data_test_new = np.array(data_test_new)  <span class="comment"># mat change to array</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        data_train_new = np.array(data_train_new)</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="comment"># 测试准确度</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        true_num = <span class="number">0</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> range(num_test):</span></pre></td></tr><tr><td class="code"><pre><span class="line">            testFace = data_test_new[i, :]</span></pre></td></tr><tr><td class="code"><pre><span class="line">            diffMat = data_train_new - np.tile(testFace, (num_train, <span class="number">1</span>))  <span class="comment"># 训练数据与测试脸之间距离</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">            sqDiffMat = diffMat ** <span class="number">2</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">            sqDistances = sqDiffMat.sum(axis=<span class="number">1</span>)  <span class="comment"># 按行求和</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">            sortedDistIndicies = sqDistances.argsort()  <span class="comment"># 对向量从小到大排序，使用的是索引值,得到一个向量</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">            indexMin = sortedDistIndicies[<span class="number">0</span>]  <span class="comment"># 距离最近的索引</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">if</span> train_label[indexMin] == test_label[i]:</span></pre></td></tr><tr><td class="code"><pre><span class="line">                true_num += <span class="number">1</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">else</span>:</span></pre></td></tr><tr><td class="code"><pre><span class="line">                <span class="keyword">pass</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">            accuracy = float(true_num) / num_test</span></pre></td></tr><tr><td class="code"><pre><span class="line">            x_value.append(pic_train_num)</span></pre></td></tr><tr><td class="code"><pre><span class="line">            y_value.append(round(accuracy, <span class="number">2</span>))</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">        print(<span class="string">'当每个人选择%d张照片进行训练时，The classify accuracy is: %.2f%%'</span> % (pic_train_num, accuracy * <span class="number">100</span>))</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</span></pre></td></tr><tr><td class="code"><pre><span class="line">    face_recognize()</span></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>Python</category>
      </categories>
      <tags>
        <tag>PCA</tag>
        <tag>人脸识别</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2020/01/19/Hello-World/</url>
    <content><![CDATA[<h1 id="Python"><a href="#Python" class="headerlink" title="Python"></a>Python</h1><p>​    <code>print(&quot;Hello World&quot;)</code></p>
<h1 id="C"><a href="#C" class="headerlink" title="C"></a>C</h1><p>​    <code>printf(&quot;Hello World&quot;)</code></p>
<h1 id="Javascript"><a href="#Javascript" class="headerlink" title="Javascript"></a>Javascript</h1><p>​    <code>console.log(&quot;Hello World&quot;)</code></p>
<h1 id="C-1"><a href="#C-1" class="headerlink" title="C++"></a>C++</h1><p>​    <code>cout&lt;&lt;&quot;Hello World&quot;&lt;&lt;endl</code></p>
<h1 id="Java"><a href="#Java" class="headerlink" title="Java"></a>Java</h1><p>​    <code>System.out.println(&quot;Hello World&quot;)</code></p>
<h1 id="C-2"><a href="#C-2" class="headerlink" title="C#"></a>C#</h1><p>​    <code>Console.WriteLine(&quot;Hello World&quot;)</code></p>
<h1 id="PHP"><a href="#PHP" class="headerlink" title="PHP"></a>PHP</h1><p>​    <code>echo &quot;Hello World&quot;</code></p>
<h1 id="Scala"><a href="#Scala" class="headerlink" title="Scala"></a>Scala</h1><p>​    <code>println(&quot;Hello World&quot;)</code></p>
]]></content>
      <categories>
        <category>Interesting</category>
      </categories>
      <tags>
        <tag>Happy Birthday</tag>
      </tags>
  </entry>
</search>
